%div
  %div= @person.name
  %div= @person.email

%div
  - if @playlists.none?
    %button#sync-playlists Sync Playlists
  - else
    %button#update-playlists Update Playlists
    #playlist-panel{data: { playlists: @playlists.to_json }}
    #playlist-target
