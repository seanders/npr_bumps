%div
  %div= @person.name
  %div= @person.email

%div
  %h2 Playlist Manager
  - if @playlists.none?
    %button#sync-playlists Sync Playlists
  - else
    #playlist-panel{data: { playlists: @playlists.to_json }}
    #playlist-target
